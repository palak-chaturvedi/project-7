from django.conf.urls import url
from . import views

urlpatterns = [
    url(r'^$', views.index, name='index'),
    url(r'^scrape/$', views.scrape, name='scrape'),
    url(r'^stock_quote/$', views.stock_quote, name='stock_quote'),
    url(r'^try_python/$', views.try_python, name='try_python'),
]


